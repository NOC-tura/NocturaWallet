/**
 * Moved to `core/presale/stagePrices.ts` so the web app quotes the same prices.
 * Re-exported here for the screens that already import from this path.
 */
export {PRESALE_STAGE_PRICES, nocUsdPriceForStage} from '../../core/presale/stagePrices';
