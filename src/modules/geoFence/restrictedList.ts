/**
 * Moved to `core/geo/restrictedList.ts` so the web app falls back to the same list.
 * Re-exported here for the app's existing importers.
 */
export {BUNDLED_RESTRICTED_LIST, BUNDLED_LIST_DATE, type RestrictedCountry} from '../../../core/geo/restrictedList';
