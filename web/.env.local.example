# Read by the Vite dev server ONLY. No VITE_ prefix, so nothing here is inlined.
# No Helius key: /rpc forwards to the coordinator's method-allowlisted route, which
# holds its own key server-side. Development needs no credential at all.
COORDINATOR_ORIGIN=https://api.noc-tura.io
